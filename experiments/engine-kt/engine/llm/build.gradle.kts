// engine/llm/build.gradle.kts — placeholder.
// Build config is inherited from `engine/build.gradle.kts` via
// its `subprojects { ... }` block (Kotlin JVM, kotlinx-coroutines,
// engine:proto dep). Add module-specific deps here as the slice
// is scoped.
