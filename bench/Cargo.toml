[package]
name = "bench"
version = "0.1.0"
edition.workspace      = true
license.workspace      = true
rust-version.workspace = true
description = "SWE-bench verification runtime for Garive — orchestrator only"

# Implementation lands as the slice is scoped. Until then,
# `cargo metadata` validates the workspace without requiring
# any source files.