//! SWE-bench verification runtime — orchestrator crate.
//!
//! Implementation lands as the slice is scoped. This stub exists
//! so `cargo metadata` / `cargo build` succeed against the
//! workspace. See `bench/AGENTS.md` for the design contract.

#![doc = include_str!("../AGENTS.md")]