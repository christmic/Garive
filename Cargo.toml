[workspace]
resolver = "2"
# Members will be added as engine/runtime slices are scoped.
# Planned members (per AGENTS.md architecture):
#
#   engine/{core, ledger, 1lm, tools, memory, knowledge, skill,
#           multiagent, scheduler, creativity, eval,
#           observability, config}
#   runtime/replica
#   cli, tui, bench
#   desktop/                  # Tauri Rust backend
#
# NOT in this workspace (different language / different repo role):
#   runtime/gateway           — Go (independent go.mod)
#   desktop/frontend          — TS/React (independent pnpm)
#   mobile                    — Kotlin KMP (independent Gradle)
#   experiments/kotlin        — Kotlin mirror (independent Gradle)
#   spec/proto                — generated Rust + Kotlin, not a member
members = []

[workspace.package]
edition = "2021"
license = "MIT"
rust-version = "1.75"