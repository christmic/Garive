[workspace]
resolver = "2"
# Members will be added as engine/runtime slices are scoped.
# Planned members (per AGENTS.md architecture):
#
#   engine/{core, ledger, 1lm, tools, memory, knowledge, skill,
#           multiagent, scheduler, creativity, eval,
#           observability, config}
#   runtime/replica
#   cli, tui, bench
#
# NOT in this workspace (different language / different repo role):
#   runtime/gateway          — Go
#   desktop, mobile          — Swift
#   experiments/kotlin       — Kotlin mirror
members = []

[workspace.package]
edition = "2021"
license = "MIT"
rust-version = "1.75"