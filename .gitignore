# Rust
target/
**/*.rs.bk

# Go
bin/
*.exe
*.test
*.out

# Swift (macOS / iOS)
.build/
.swiftpm/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.dSYM/
*.dSYM.zip

# Kotlin / JVM
.gradle/
build/
.idea/
*.iml
*.class
*.jar
!gradle/wrapper/gradle-wrapper.jar
local.properties

# TypeScript / Node
node_modules/
.next/
out/
dist/
.turbo/
*.tsbuildinfo

# Virtual environments
.venv/
venv/
env/

# Editor / OS noise
.DS_Store
*.swp
*.swo
*~
.vscode/

# Logs and coverage
*.log
coverage/
*.lcov

# Local secrets / config
.env
.env.local
*.local

# Tooling caches
.cache/
.mypy_cache/
.ruff_cache/
.pytest_cache/
# Cargo lock for empty / application-only workspaces (Garive
# currently has no binary crates, so we ignore it; reinstate
# tracking when the workspace starts producing binaries).
Cargo.lock
